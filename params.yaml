train:
  test_size: 0.2
  random_state: 42
  n_estimators: 300
  min_samples_leaf: 2

monitoring:
  anomaly_contamination: 0.05
  drift_psi_threshold: 0.20

serving:
  model_path: artifacts/forgepulse.joblib
